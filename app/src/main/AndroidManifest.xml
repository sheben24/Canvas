<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_id"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_id_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".SurfaceEditActivity" />
        <activity android:name=".SurfaceOriActivity" />
        <activity android:name=".ClippingEditActivity" />
        <activity android:name=".ClippingOriActivity" />
        <activity android:name=".DrawEditActivity" />
        <activity android:name=".DrawOriActivity" />
        <activity android:name=".CreateEditActivity" />
        <activity android:name=".CreateOriActivity" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>